export interface QuircData {
    version: number;
    eccLevel: number;
    mask: number;
    dataType: number;
    eci: number;
    payload: ArrayBuffer;
    text?: string;
}
export interface QuircResult {
    corners: [[number, number], [number, number]];
    data: QuircData;
}
export declare class QuircError extends Error {
    constructor(err: string);
}
export declare class Quirc {
    private instance;
    private exports?;
    private quircPtr?;
    private width;
    private height;
    private codePtr?;
    private dataPtr?;
    private textDecoders;
    constructor(instance: WebAssembly.Instance);
    private getTextDecoder;
    decode(img: Uint8Array | Uint8ClampedArray | ArrayBuffer, w: number, h: number): QuircResult[];
}
export default Quirc;
